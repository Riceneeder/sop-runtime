/**
 * @packageDocumentation
 *
 * Public entrypoint for the validator package.
 *
 * `@sop-exec/validator` 的公共导出入口。
 */
export type {Diagnostic, ValidationResult} from './diagnostic';
export {validateDefinition} from './validate_definition';
