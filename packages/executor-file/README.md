# @sop-runtime/executor-file

File executor adapter for sop-runtime — reads/writes files within workspace root as SOP steps.

See [`src/file-executor.ts`](./src/file-executor.ts) for the implementation and
[`../../scripts/smoke_file.ts`](../../scripts/smoke_file.ts) for a runnable smoke test.
