# @sop-runtime/executor-http

HTTP executor adapter for sop-runtime — makes `fetch()` requests as SOP steps.

See [`src/http-executor.ts`](./src/http-executor.ts) for the implementation and
[`../runtime/scripts/smoke_http.ts`](../runtime/scripts/smoke_http.ts) for a runnable smoke test.
